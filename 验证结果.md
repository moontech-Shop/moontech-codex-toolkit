# 整合包验证结果

版本：1.2.4；日期：2026-09-21。

- 三个网站 Skill 均通过官方 quick_validate 结构校验。
- 相对Markdown文件引用检查通过，包内Skill未包含作者机器的用户目录或项目绝对路径。
- 隔离目录首次安装3个Skill，第二次3个均识别为相同内容并跳过。
- 修改其中一个已安装Skill后，默认安装拒绝整批写入，原修改保留。
- 显式替换先备份，备份中的自定义文件仍存在；安装后逐文件哈希一致。
- 从安装后的初始化脚本生成21个项目文件，重复初始化新增文件0个、目录0个，已有内容全部保留。
- 初始化脚本在首次写入前检查全部目标目录、父目录和模板文件类型冲突；碰到冲突时没有留下半初始化目录。
- 初始状态明确写为“任务待创建或核实”；创建、复用、归档阻塞和不可用状态必须分别登记，不能提前宣称三个工作台已经完成。
- 包内提供v1.2标准、手册和按需加载的持久任务提示；缺少持久任务工具时不会生成虚假任务ID，也不会用普通文件或临时子代理冒充任务。

未验证：新版任务登记与状态同步的完整桌面端三任务创建流程、其他机器环境、浏览器/Shopify/Git账户接入及真实网站验收。Skill结构、脚本幂等、冲突预检、包校验和隔离安装在本机验证。

安装需要PowerShell 7。脚本默认使用官方文档的用户 .agents/skills 目录，检测到旧 .codex/skills 里的同名包技能则整套沿用旧目录；两个目录同时存在包技能时会拒绝，避免相对依赖断裂。现代目录发现规则依据 https://developers.openai.com/codex/skills/；旧目录为现有环境兼容路径，不作为新机器推荐默认。
